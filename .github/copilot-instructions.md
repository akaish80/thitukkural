# Copilot Instructions for Thirukkural Vite Project

- This project is a Vite-based React + TypeScript migration of a previous Create React App (CRA) project.
- All code, components, and logic have been ported to the new structure.
- Use Vite commands (`npm run dev`, `npm run build`, etc.) for development and builds.
- SCSS, modular CSS, and Redux Toolkit are supported.
- Refer to the updated README.md for usage and migration notes.

## Development Guidelines
- Use the `src/` directory for all source code.
- Place static assets in `public/`.
- Follow Vite and React best practices for new features.
- For legacy code or migration issues, consult the original CRA README (included below the migration notes in README.md).

---

This file is auto-generated as part of the migration process. Clean up or update as needed for your workflow.
